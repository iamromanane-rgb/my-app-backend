spring.application.name=backend

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=iamromanane@gmail.com
spring.mail.password=kbyt wlaf dwdt emwc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

server.port=9090

# JWT Configuration
jwt.secret=${JWT_SECRET:change-me-to-a-strong-secret-key-32-chars-min}
jwt.expiration-ms=3600000

# Scheduler Configuration
scheduler.notifications.cron=0 0 6 * * *

# Admin Configuration
admin.emails=${ADMIN_EMAILS:romananedandapani@gmail.com}
